/**
 * Prayer times TypeScript types.
 * Re-exported here for clean developer imports: `import { PrayerTimes } from '@/types/prayer'`
 */
export type {
  PrayerTimes,
  HijriDate,
  PrayerTimesResponse,
} from '@/services/prayerService';
