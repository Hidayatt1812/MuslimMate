/**
 * Ibadah tracker and Tahfidz TypeScript types.
 * Re-exported here for clean developer imports: `import { DailyTracker } from '@/types/tracker'`
 */
export type {
  BookmarkItem,
  LastReadData,
  DailyTracker,
  StreakData,
  TahfidzPlan,
} from '@/services/storageService';
